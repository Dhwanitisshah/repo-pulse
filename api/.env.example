REDIS_ADDR=localhost:6379
CORS_ORIGINS=http://localhost:5173,http://localhost:4173
STREAM_NAME=events
MAX_EVENTS=50
CONSUMER_GROUP=pulse-workers
# CONSUMER_NAME=   # defaults to $HOSTNAME, or a random suffix if unset
